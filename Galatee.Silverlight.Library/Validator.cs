using System;
using System.Net;
using System.Windows;
using System.Windows.Controls;
using System.Windows.Documents;
using System.Windows.Ink;
using System.Windows.Input;
using System.Windows.Media;
using System.Windows.Media.Animation;
using System.Windows.Shapes;
using System.Text.RegularExpressions;

namespace Galatee.Silverlight.Library
{
    public class Validator
    {
        /// <summary>
        /// Ramène true si la valeur est numérique
        /// </summary>
        /// <param name="pThis"></param>
        ///// <returns></returns>
        //public  bool IsNumericValue(this string pThis)
        //{
        //   var regex = new Regex(@"[0-9]{1}", RegexOptions.Multiline | RegexOptions.IgnorePatternWhitespace);
        //   return (regex.Matches(pThis).Count >= pThis.Length);
        //}
        ///// <summary>
        ///// Ramène true si la longueur de la variable est inférieur à pValue
        ///// </summary>
        ///// <param name="pThis"></param>
        ///// <param name="pValue">longueur servant de référence</param>
        ///// <returns>Valeur booléenne</returns>
        //public   bool IsLenghtInferior( this string  pThis,int pValue)
        //{
        //    return (pThis.Length < pValue);
        //}

        //public  bool IsStringValue(string pValue)
        //{
        //    var regex = new Regex(@"[0-9]{1}", RegexOptions.Multiline | RegexOptions.IgnorePatternWhitespace);
        //    return (regex.Matches(pValue).Count > 0);
        //}
    }
}
